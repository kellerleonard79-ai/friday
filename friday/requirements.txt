requests>=2.31.0
pyyaml>=6.0
python-telegram-bot[job-queue]>=20.0
google-genai==2.0.0
rumps>=0.4.0
# Fast Apple Calendar reads. Without it the reader falls back to JXA, which
# costs ~35 ms per event in the scanned calendar — minutes per briefing on a
# busy shared calendar. See connectors/apple_calendar.py.
pyobjc-framework-EventKit>=10.0; sys_platform == "darwin"
icalendar>=5.0
PyMuPDF>=1.24
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.0
